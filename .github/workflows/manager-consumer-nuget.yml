on:
  push:
    paths:
      - 'manager/protocol/**'
      - '.github/workflows/manager-consumer-nuget.yml'
  workflow_dispatch:

jobs:
  publish:
    name: Pack & Publish nuget
    runs-on: ubuntu-latest
    steps:
      - uses: astorDev/versy/calver/nuget@main
        with:
          project: manager/consumer/lib/Confi.Manager.Consumer
          nuget-api-key: ${{ secrets.NUGET_API_KEY }}